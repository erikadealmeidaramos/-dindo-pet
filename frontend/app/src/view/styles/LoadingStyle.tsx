import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  indicatorWrapper: {
    alignItems: 'center',
    justifyContent: 'center',
  },
  indicator: {
    marginTop: '10%',
  },
});
