import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  text: {
    justifyContent: 'center',
    alignContent: 'center',
  },
  title: {
    justifyContent: 'center',
    alignContent: 'center',
  }
});
